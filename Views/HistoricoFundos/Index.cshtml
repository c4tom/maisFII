@model IEnumerable<MaisFII.Models.HistoricoFundo>

@{
    ViewData["Title"] = "Índice";
}

<h1>Index</h1>

<p>
    <a class="btn btn-success" asp-action="Create">Novo Registro</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.valor)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Data)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.valor)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Data)
            </td>
            <td>
                <a class="btn btn-primary" asp-action="Edit" asp-route-id="@item.HistoricoFundoId">Editar</a> |
                <a class="btn btn-primary" asp-action="Details" asp-route-id="@item.HistoricoFundoId">Detalhes</a> |
                <a class="btn btn-danger" asp-action="Delete" asp-route-id="@item.HistoricoFundoId">Remover</a>
            </td>
        </tr>
}
    </tbody>
</table>
