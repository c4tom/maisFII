@model IEnumerable<MaisFII.Models.HistoricoFundo>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.valor)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Data)
            </th>
            <th>
                Sigla/Fundo
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.valor)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Data)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Fundo.Sigla)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.HistoricoFundoId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.HistoricoFundoId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.HistoricoFundoId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
